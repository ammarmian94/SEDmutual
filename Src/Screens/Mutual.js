import React from 'react';
import {
  Text,
  View
} from 'react-native';

const Mutual = ({navigation}) => {
  return(
    <View style={{
        alignItems: 'center'
      }}>
        <Text style={{
          fontSize: 30,
        }}>Mutual Screen</Text>
      </View>
  );
};

export default Mutual;